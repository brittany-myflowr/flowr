import { todayMeshGradients } from '@/constants/todayMeshGradients';

/** Soft brand mesh for splash and launch — same planner glow as Today morning. */
export const brandMeshGradient = todayMeshGradients.morning;
